// src/app/account/pageInner.tsx
'use client'

import { useSearchParams } from 'next/navigation'
// ... other imports

export default function AccountInner() {
  const params = useSearchParams()
  // ... your existing logic that used useSearchParams
  return (
    // ... your JSX
  )
}
